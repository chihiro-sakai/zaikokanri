{
  "name": "zaikokanri-training",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute zaikokanri-training --local --file=schema.sql",
    "db:remote": "wrangler d1 execute zaikokanri-training --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
